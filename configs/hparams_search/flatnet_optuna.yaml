# @package _global_

# hyperparameter optimization for FlatNet model with Optuna:
# python train.py -m hparams_search=flatnet_optuna experiment=flatnet_experiment

defaults:
  - override /hydra/sweeper: optuna

optimized_metric: "val/top1_acc_best"

hydra:
  mode: "MULTIRUN"

  sweeper:
    _target_: hydra_plugins.hydra_optuna_sweeper.optuna_sweeper.OptunaSweeper
    storage: null
    study_name: null
    n_jobs: 1
    direction: maximize
    n_trials: 20

    sampler:
      _target_: optuna.samplers.TPESampler
      seed: 1234
      n_startup_trials: 10

    params:
      model.optimizer.lr: interval(0.0001, 0.001)
      model.optimizer.weight_decay: interval(0.00001, 0.001)
      model.eegnet.p: interval(0.3, 0.7)
